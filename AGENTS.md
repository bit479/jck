# 项目开发规则

## 用户情况

项目负责人没有编程基础，因此代码必须清晰、简单、方便后续修改。

## 技术限制

* 只使用HTML、CSS和原生JavaScript。
* 不使用React、Vue、Node.js、数据库和后台系统。
* 不随意安装第三方依赖。
* 网站需要适配平板横屏和电脑浏览器。
* 优先保证功能稳定，再增加动画效果。
* 所有国家和项目信息统一放在`js/data.js`中。
* 不得虚构项目金额、建设周期、设备数量和其他未经确认的数据。
* 缺少内容时使用明确的占位文字。

## 工作方式

* 每次只完成当前指令要求的功能。
* 不要提前开发下一阶段功能。
* 修改代码前先检查现有文件。
* 修改完成后检查HTML、CSS和JavaScript是否存在明显错误。
* 每次完成任务后，列出新增或修改的文件。
* 用中文说明测试方法。
* 不要删除已经能够正常运行的功能。
* 如果需求有不确定之处，优先采用最简单、最容易维护的实现方案。
* 地图、导航和弹窗必须兼容鼠标点击和触摸操作。
* 所有按钮应适合平板触摸，重要按钮高度不低于48px。

## 项目说明

完整需求位于根目录的`PROJECT_SPEC.md`。

每次执行任务前，应先阅读该文件，但只完成当前指令明确要求的阶段。
